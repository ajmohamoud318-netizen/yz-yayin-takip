# =====================================================================
#  YZ Yayın Takip — environment variables
#  Copy to `.env` and fill in real values. Never commit `.env`.
#  Dokploy reads these from its UI; locally they're loaded by Vite.
# =====================================================================

# ----- Public app config (exposed to the browser via Vite) -----

# Where the browser talks to the backend once it exists.
# Empty by default: Vite proxy at /api → http://localhost:4000 is used in dev.
VITE_API_BASE_URL=

# ----- Server-only (Dokploy / production secrets) -----

# Backend base URL — used by the future Fastify server, not the SPA.
API_BASE_URL=http://localhost:4000

# OAuth (planned; backend not deployed yet)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# PostgreSQL connection string for the planned Fastify backend.
DATABASE_URL=

# Redis (sessions, cache, pub-sub)
REDIS_URL=

# Nodemailer — invitation + stage-change notifications
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="YZ Yayın Takip <noreply@yukselenzeka.com>"

# Cookie signing secret (planned)
SESSION_SECRET=
