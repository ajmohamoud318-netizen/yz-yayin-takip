Push subscription requires a user gesture — `subscribe()` must be called from a click handler or Safari will throw. iOS Safari only supports push inside an installed PWA (Home Screen app); the hook reports `needs-install` with a 4-step Turkish instruction list instead of prompting for permission. Desktop devices are intentionally not offered push setup unless they already have an existing subscription.