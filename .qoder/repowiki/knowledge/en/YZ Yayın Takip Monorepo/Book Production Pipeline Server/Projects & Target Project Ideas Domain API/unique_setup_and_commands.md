The `/projects/import` endpoint supports a `dryRun` mode that performs all duplicate/validation checks inside a transaction and then throws a custom error carrying the summary so the caller can roll back without side effects. Image upload routes enforce per-file size limits (`MAX_IDEA_IMAGE_BYTES`, `MAX_GALLERY_IMAGE_BYTES`) by streaming chunks and rejecting oversized payloads before disk write.